-----PYTHON SOCKETS-----

Learnt:
    socket(), bind(), listen(), accept()
    connect(), send(), recv()
    encode() / decode()
    Blocking behaviour
    socket.SOL_SOCKET, socket.SO_REUSEADDR

Mini-project: Echo server — server receives whatever the client sends and bounces it back. Your first working server/client pair.